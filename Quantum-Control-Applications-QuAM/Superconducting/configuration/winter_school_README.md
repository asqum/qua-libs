Run `make_wiring_lffem_mwfem_INIT.py` to create the base element of QUAM state for OPX1000 with MW-FEM and LF-FEM.

Then run `make_quam.py` to populate it with initial dynamic parameters.

Finally, run `modify_quam.py` to set initial guesses for qubit parameters. 